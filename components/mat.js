import React from 'react';
import { View } from 'react-native';

const mat = () => {
  return (
    <View
      style={{
        width: 694,
        height: 52,
        top: 616,
        left: 344,
        borderRadius: 26, // Half of the width and height
        backgroundColor: 'red'
      }}
    />
  );
};

export default mat;